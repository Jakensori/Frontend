import 'package:flutter/material.dart';
class MealChartPage extends StatelessWidget {
  const MealChartPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child:Text('Meal Chart Page')
    );
  }
}
